pipeline {
    agent any
    stages {
            stage('Display Build info') {
                steps {
                    echo "BUILD_NUMBER is: ${BUILD_NUMBER}"
                }
            }
    }
}
