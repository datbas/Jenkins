pipeline {
    agent any
    stages {
            stage('Display Build info') {
                steps {
                    sh "echo BRANCH_NAME: ${BRANCH_NAME}"
                }
            }
    }
}
