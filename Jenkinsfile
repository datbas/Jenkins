pipeline {
    agent any
    stages {
            stage('Display Build info') {
                steps {
                    sh "echo BUILD_NUMBER: ${BUILD_NUMBER}"
                }
            }
    }
}
